# Backlog

**Role:** the *execution layer* beneath the sprint plan docs. The plans —
[architecture_cleanup.md](roadmap/architecture_cleanup.md) (`clean`) and
[graphics_fidelity.md](roadmap/graphics_fidelity.md) (`gfx`) — are **strategy**: what to
build, why, in what order, with the full rationale. This file is the
**operational queue** that answers **"what's next?"** — concrete, scoped,
status-tracked items in a rolling near-term window. Agent-maintained: kept in
sync as work lands.

**How this file is driven** — see the `steer` skill
(`.claude/skills/steer/SKILL.md`) for the full procedure. In short:

- **"what's next?"** → the agent picks the top pickable item, scopes it into a
  brief, and stops for your go.
- **"add X / fix Y"** → filed here as a `next` item, then done as normal work.
- **vision / direction talk** → captured in the right plan doc (ADR if a fork
  resolves), then decomposed into items here.

**Status vocabulary:**

- `next` — ready, no unmet dependency; the pool "what's next?" picks from.
- `wip` — in progress.
- `verify` — **landed compile+clippy-clean but not runtime-verified.** The
  thalos-specific status: agents can't run the game, so landed work waits on
  either a headless probe (`just screenshot` / `just preview` / `just
  ui-preview` — agent-servable) or a user play session + screenshot. A `verify`
  item's row names which. Not `done` until observed working.
- `blocked` — waiting on another item or an external / user action (named in
  **Deps**).
- `done` — complete and verified (date in Notes).
- `later` — real but not now; listed for dependency context, not yet pickable.

**Conventions:**

- **IDs are stable and chronological.** Reuse the plan docs' own IDs where they
  exist (packages `CL-A`…`CL-G` from `clean §3`, `F1`–`F9` / `W`-numbers / `TM` /
  `C1` from `gfx` — those are owned by one document, so they don't collide). For
  an item with no plan-doc home, mint `BL-YYYYMMDDTHHMMSSZ-slug` from the current
  UTC time (`date -u '+%Y%m%dT%H%M%SZ'`). **Never allocate the next `BL-n`** — a
  shared counter makes parallel branches mint the same ID for unrelated items,
  and those IDs are cited from Rust source, so a collision becomes a wrong
  permanent reference (ADR-20260722T170714Z-one-chronological-identity-rule).
  `BL-1`–`BL-40` are frozen legacy IDs: cite them, never extend or renumber them.
  Never renumber anything; mark `done`, don't delete — the queue is also the
  record of what was done.
- **Rolling window.** Only the two active sprints are tracked at item
  granularity. The graphics later-sprint pool stays at doc granularity in
  `gfx §4`; pull items in here as they near.
- **One source of truth per fact.** Rationale and design stay in the plan docs /
  specs / ADRs; this file holds status + scope pointers, not re-explained
  reasoning. Flip the plan doc's checkbox and this row **in the same change**.
- **Discovered work becomes a row, never a silent TODO.**
- **Refs:** `clean §N` = architecture_cleanup.md · `gfx §N` =
  graphics_fidelity.md · `ADR-YYYYMMDDTHHMMSSZ-slug` = adr/ ·
  `INC-YYYYMMDDTHHMMSSZ-slug` = incidents/ (plus frozen legacy `INC-NNNN`) · other
  docs by filename.

> Statuses below were seeded 2026-07-18 from the plan docs (whose checkboxes
> date to 2026-07-05) plus auto-memory. First `steer` invocation should
> reconcile against `jj log` + the working tree.

---

## Track 1 — Architecture & code quality (primary sprint · `clean`)

Goal (`clean §1`): one canonical path per operation, N-by-default, in-flight
unifications finished. Packages land compile+clippy-clean, update the relevant
spec, and end with a user verification checklist.

| ID | Item | Status | Est | Deps | Refs |
|----|------|--------|-----|------|------|
| CL-B | GameContext Phase 3 — invert ownership (`NextState<GameContext>` sole writer, `ContextHistory` stack) | verify | — | **user checklist** in clean §3.B (menu↔hub↔VAB↔flight matrix) | clean §3.B · ui_flow.md |
| CL-C | One craft-placement core (`place_craft` in `spawn.rs`) | verify | — | **user checklist** in clean §3.C (all spawn/respawn/relaunch/launch paths; watch for teleport jitter) | clean §3.C |
| CL-C2 | Route map/debug teleports through `place_craft` — fixes the flagged candidate bug (map cmd-click teleport never clears the Avian bubble → phantom velocity) | next | S | CL-C verified first (the reorder needs runtime confidence that `transition_authority` doesn't snapshot state) | clean §3.C |
| CL-D | **Structures become the one placement layer**: split `runway.rs` (geometry → `structures/runway_geometry.rs`, placement → the C core, collider/anchoring/spaceport → `structures/`); promote connections into `StructureRegistry` (structurally fixes grass-under-taxiway); extract shared f64 `snap_to_body_surface` (align with `ViewAnchor`'s `cam_world`/`ground_world`, don't duplicate) | next | L | — | clean §3.D · base_building.md |
| CL-E1 | `ActiveCraft(Option<Entity>)` accessor seam + `track_active_craft` | verify | — | was blocked on an unrelated `thalos_ui` WIP — re-check it builds, then full scenario matrix (pure add, nothing observable should change) | clean §3.E |
| CL-E2 | Incremental per-craft-state migration (`GearState`/`ParkingBrake`/`EvaMode`/`RealizedControl`/`ManeuverPlan` → craft-entity components; `.single()` sites → `ActiveCraft`) | later | — | requirements-driven: do when a second craft actually exists | clean §3.E kept-singleton ledger |
| CL-G | UI kit runtime interaction pass (hover / drag / typing / hangar flows) | verify | — | **user session** — headless kitchen-sink PNG already checked | clean §3.G · ui.md |
| CL-A | Dead bake-pipeline purge — rescoped to function-level surgery on the `compile_terrain_config` chain | later | L | natural time = the terrain-generator rework | clean §3.A |
| CL-B2 | HUD hide/restore → `OnEnter`/`OnExit(GameContext)`; delete the `.open` mirrors once no reader needs them | later | M | CL-B verified | clean §3.B |
| CL-C3 | Placement helper dedups: one craft-clearance measurer, generic deferred-placement gate. (The shared cursor→pad raycast may already be done — `cursor_body_dir` landed 2026-07-05; re-verify at pickup) | later | S | — | clean §2.1 |
| CL-F | Small unifications, batch as touched: document the terrain-height three-mirror design at `terrain_registry.rs`; `camera.rs` submodule split only when camera work next happens | later | S | — | clean §3.F |
| BL-25 | Freecam warp stability — latch the current `ViewAnchor` body on entry, store the complete camera pose body-fixed, and reproject it after each solar-system update so neither position nor heading drifts under warp; inertial fallback when no terrain-backed view body exists | verify | S | **user:** `just game orbit` → F4, raise warp, and confirm a parked surface/heading stays fixed; move the camera, then repeat | clean §3.F · `freecam.rs` · simulation.md · 3 focused tests passed 2026-07-21 |

## Track 2 — Graphics fidelity (secondary sprint · `gfx`)

Goal (`gfx §2.3/§3`): the one-world principle via the F1–F9 unification
foundation. F1/F2 verified; F3–F6 landed awaiting calibration; F7–F9 next
sprint.

| ID | Item | Status | Est | Deps | Refs |
|----|------|--------|-----|------|------|
| GF-CAL | **Screenshot calibration & verification sweep** for everything landed-☑: noon exposure (`GLOBAL_EXPOSURE_STOPS`, W9 flux constant), SSAO tuning (F5 dials), one-shadow-world screenshots (F6/W5/W6 bias knobs), landcover palette, moonlight, clump cards. Start agent-side with `just screenshot` presets; queue what needs a live eye for the user | next | M | — | gfx §3, §4.1–4.3 · shadow_unification_prompt.md |
| TM-P1 | Macro landcover Phase 1 — moisture in f64 `ProceduralSurface` → albedo-attachment alpha | verify | — | landed 2026-07-18; screenshot pass | terrain_macro.md |
| TM-P2 | Climate MVP (terrain_macro Phase 2) — latitude cold-lift descends eco bands (polar ice caps, tundra), warmth-gated sand palette, moisture geography (equator/subtropic/mid-lat belts + continentality); shader latitude via `compute_local_position` | verify | — | landed 2026-07-18; world_map-verified, needs live-eye pass | terrain_macro.md §4 |
| TM-P2r.1 | Scatter/biome coupling — **trees & shrubs × moisture + treeline**: `woody_biome_gate` in `body_render::ground::scatter` multiplies the woody accept by a moisture dryness ramp (mirrors the ground's `vegetation_color` forest transfer) × a cold-lift-descended treeline term, so trees thin on dry steppe, vanish on bare desert, and stop at the poles instead of ignoring climate. Landed 2026-07-20, compile + scatter-tests clean | verify | — | **user:** orbit/cruise over a dry belt + a polar site — woody cover should follow the ground palette | terrain_macro.md §3, §4 |
| TM-P2r | Phase 2 remainder (post TM-P2r.1): grass profile per biome, explicit biome weights if needed, sea ice, regional relief character | later | M | TM-P2 verified | terrain_macro.md §4 |
| BL-11 | Whole-planet biome map export (`just map`): mercator (default) / equirect, true in-game macro palette render + flat `MacroBiome` class map + area-weighted per-biome coverage, dryness-histogram and per-latitude-band stats. `ProceduralSurface::sample_biome_d` classifies from the **same band weights the albedo blends** (`macro_band_ts`, output-identical refactor of `albedo_at`). *(Renumbered from a colliding BL-9 — the water-field work minted BL-9/BL-10 concurrently.)* | done | — | — | terrain_macro.md · world_map.rs · PNGs read 2026-07-20 |
| BL-12 | Celestial backdrop daylight suppression follows the render camera: `update_atmospheric_star_visibility` now consumes the canonical body-fixed `ViewAnchor`, so freecam, flight, god-view, and screenshot cameras evaluate twilight and the Kármán transition at the actual observer. Landed 2026-07-20; focused tests + `cargo check -p thalos_game` clean | verify | S | **user:** keep a craft on the daylight surface, freecam above the Kármán line (backdrop visible), then descend through it (backdrop fades according to the camera) | gfx §2.3 one-view-anchor invariant · `sky_render.rs` |
| BL-13 | **Earth-reference atmosphere comparison:** deterministic 3:2 ISS-like land-only orbital screenshot preset and matched custom-vs-Bevy evidence. The Bevy promotion was reversed after the surface/orbit result and live distance stability failed; BL-30 owns the custom-only replacement | done | M | superseded by BL-30 / ADR-20260721T185221Z-custom-rocky-atmosphere | atmosphere.md · gfx §2.3/§4.5 |
| BL-14 | **Fast, reliable Bevy dev loop:** route game, headless screenshots, object preview, and UI preview through one dynamic-link feature set; move temporary wgpu driver counters behind opt-in `gpu-counters` so preview/game reuse one dylib; fix the cross-platform screenshot env recipe; retire the unstable local `-Zthreads=8` experiment after its MIR ICE poisoned incremental objects; document one-target/one-Cargo-process hygiene | done | S | one-time feature/config fingerprint rebuild completed (196.9 s); default dynamic game graph compile-clean; all four recipes dry-run clean | tooling.md · justfile · local `.cargo/config.toml` · INC-0006 |
| BL-15 | **Photographic daylight star suppression:** the Earth-reference orbit frame is correctly above the Kármán line, but the exposed starfield is much brighter than the daylight reference. Extend BL-12's camera-anchored atmospheric visibility with the shared exposure/sky-luminance response; do not hide stars merely because a planet is in frame | later | S | after BL-13 live verification / F7 exposure calibration | gfx §2.3 · `sky_render.rs` |
| BL-16 | **Mira EVA transparent horizon stipple:** the canonical eye-level `mira-eva` probe reproduces the ridge artifact after a 1,200-frame warm-up. Fullbright proves complete opaque raster coverage; culling, provider, tile lookup, self-shadow, external-shadow, and height-normal-mip A/Bs do not remove it; geometric-normal rendering does. Root cause: Mira's metre-scale procedural regolith colour and micro-normal octaves were distance-faded but not filtered against the fragment's body-space pixel footprint, so grazing terrain compressed them below Nyquist and Hapke amplified the unresolved normal flips into bright/dark stipple. Regolith detail now uses the shared per-octave footprint fades; f64 crater distance also removes an independent compatibility-producer height quantisation defect. | done | M | matched `terrain-regolith-filter` A/B inspected; cache-disabled `mira-eva`, `mira-surface`, and `mira-orbit` captures | mira_airless_mvp.md · INC-0009 |
| BL-18 | **Deterministic visual comparison harness:** `just compare <preset> <axis>` runs typed variants as isolated headless game processes and emits full-resolution captures, labelled contact sheet, baseline diffs/wipes, metrics, and a provenance manifest. Its direct child launcher reproduces Cargo's complete dynamic-library path (profile, deps, Rust target-libdir; INC-0008). First axes: atmosphere A/B and SSAO off/on/raw multi-test | done | M | runtime-verified 2026-07-21: 2-way atmosphere + 3-way SSAO artifacts inspected | visual_testing.md · gfx §4.10 · ADR-20260721T032344Z-isolated-headless-visual-comparisons · INC-0008 |
| BL-19 | Extend visual comparison axes with shared debug channels as their shader instrumentation lands: normals, depth, terrain LOD/tile IDs, shadow factor/cascade, material IDs, atmosphere transmittance, direct/ambient/specular lobes. Consider an interactive image wipe only if the artifact loop proves too slow | later | M | BL-18 | visual_testing.md · gfx §4.10 · ADR-20260721T032344Z-isolated-headless-visual-comparisons |
| BL-20 | **Visual comparison must fail on render-pipeline errors:** `just compare runway-atmosphere atmosphere` accepted two images after Bevy logged a fatal WGSL pipeline validation error, producing atmosphere over missing terrain. Route render/shader compilation failures into a headless non-zero exit or machine-readable failure marker and refuse to assemble artifacts from invalid variants | next | S | BL-18 | visual_testing.md · 2026-07-21 runway probe |
| BL-21 | **Rocket engine plumes (Phases 1–2):** data-driven, pressure-responsive liquid-plume renderer replacing the (post-F6 silently-dead) engine-mesh tint. Typed `PlumeSignals` boundary on the engine → propellant preset + `log2(pressure_ratio)` curves → `PlumeParams` → `plume.wgsl`. **Phase 2 (2026-07-22)** made emission *physical*: one thermodynamic chain (free-expansion cone → mass conservation → adiabatic **and** entrainment cooling → Wien-side band emission → emission through an absorbing column) so brightness follows from shape and pad→orbit needs no regime switch. Real `p_exit` from `Engine::optimized_for`; pressure-blended propellant palettes (methalox reads blue-violet in vacuum, orange-white afterburning at sea level); anisotropic streak noise; envelope-tracking billboard. Fixed INC-0020 (repurposed uniform lane erased the vacuum plume). Later phases: particles, heat-haze distortion, engine lights, ground FX, jet afterburners, solid-motor cloud path. | verify | — | **agent:** static look screenshot-verified at vacuum / 18 kPa / sea level via `just screenshot plume`; **user:** live throttle + ignition transients, in-flight pressure sweep on a real ascent | plume.md · INC-0020 |
| BL-40 | **Analytic plume light in the lighting spine:** a firing engine must illuminate its own craft (the strongest 'the plume belongs to the vehicle' cue in the reference imagery). Bevy `PointLight` **cannot** be used — the flight camera's 0.5 m → 1e11 m depth range makes clustered forward lighting degenerate, so a point light contributes exactly zero regardless of intensity (INC-0021, measured). Fix is an analytic term alongside the existing sun/moonlight terms: publish per-craft plume light state (position, colour, luminous power) from `PlumeSignals` into `SceneLighting`, evaluate in `ship_part.wgsl` + `shadowed_standard.wgsl`. Wants an ADR — it is a step of the F-series craft/spine unification (gfx §3), not a plume-local change. | next | M | — | plume.md · INC-0021 · graphics_fidelity.md §3 |
| TM-P3 | **Biome rebalance — the planet reads as a real planet, authored to lore** (35 % land, geologically old/low-riding, lush with rust-red laterite where cover thins — lore §II). Landed 2026-07-20, `GENERATOR_VERSION` 12: (1) macro altitude bands re-aligned to the ground's eco bands (upland 1500–2400 eco, rock 2400–3000, snow 3000–3600 — was upland-from-**120 m**, which claimed 59 % of land and crushed the climate palette); (2) **INC-0005** — `.max(EPSILON)` smoothstep guard inverted the forest term (forest painted on the *driest* ground since TM-P1); (3) tundra moved between upland and rock in the chain; laterite soil step added (mirrors ground `C_SOIL`); (4) land lowered (platform 420→300 m, interior gain 650→400 m) + `CONTINENT_C0` 0.143 → land 35.2 %; (5) subtropic belt 0.40→0.70 + real continentality gate; (6) **ecotone mosaic gate** (v13, from the user's orbital screenshot — dry belt read as splotchy camo): 90 km/9 km moisture tiers fade to 35 % where the latitude+continental trend has committed, so belt cores are coherent and patchwork lives at climate transitions. Map-verified: forest 30/grass 26/steppe 19/desert 4.6/tundra 2/snow 3.6 % of land, coherent belts in the right latitudes, runway site LAND at 602 m. **Note:** coastlines reshaped by the C0 change — the pending water/coast live-eye (new BL-9) sees new coasts. **Next slice:** TM-P3b | verify | — | **user:** orbit + descent over a dry belt and the polar gradient; `just screenshot` presets agent-side first | terrain_macro.md §4 · INC-0005 · lore/solar_system.md §II |
| TM-P3b | Biome *identities* on top of the TM-P3 balance: erg/reg desert character (dunes vs gravel), savanna between forest and steppe, taiga vs temperate forest tone, softer polar rock ring (wider tundra), sea ice at the polar ocean; scatter coupling folds in via TM-P2r | later | M | TM-P3 verified | terrain_macro.md §4 |
| MIRA-MVP | **Playable offline-package Mira compatibility slice:** standalone `just bake Mira`; 31.4 MiB content-keyed deterministic airless package; one N-body `BodySurfaceRegistry` authority through ground/impostor/height/physics; package-aware tile-cache namespace; `mira`/`mira-eva` routes; crater-targeted `mira-orbit` + `mira-surface` headless probes; airless scatter gate + calibrated shared Hapke regolith | verify | — | — | checked-in package key `4fed789a18a2ddda` is stale against expected `84094ad51a76cb37`, blocking runtime/capture boot 2026-07-21; rebuild with `just bake Mira`, then rerun capture · mira_airless_mvp.md §1 · ADR-20260720T211046Z-offline-terrain-packages |
| MIRA-0 | **Production terrain-package tracer:** schema-v1 magic/manifest + producer/model identity; 32→512 five-level adaptive height pyramid with six R16 roots, quantized signed residuals, canonical half-open ownership, 2,047 logical nodes / 1,961 blobs, 86 ancestor fallbacks within a 256 m compatibility budget; predictor/reconstruction metrics; checksum/overlap/hierarchy/error/content validator; exact-artifact tile-cache fingerprint; `PackageSurface` reader. Deterministic SHA-256 and cache-disabled orbit/surface captures verified 2026-07-20; no seams/fallback holes | done | — | MIRA-MVP | mira_airless_mvp.md §5, §8–§9 · ADR-20260720T211046Z-offline-terrain-packages |
| MIRA-1 | **Airless diffusion patch proof (roadmap L1–L2):** Rust/Burn 0.21 CPU/WGPU/CUDA denoiser + DDPM/DDIM contract; explicit source/split/physical-scale sample vocabulary; deterministic labelled synthetic corpus; checksum-gated Kaguya/SLDEM preparation; SafeTensors, EMA, exact resume, overlap fusion; 11-channel residual U-Net. **L1 passed 2026-07-21:** real Kaguya overfit reconstructed recognisable morphology at 7.62 m RMS with exact repeat. **L2 v3 failed:** terminal-SNR-correct epsilon amplified low-SNR error into diagonal noise (104.93 m RMS). **Controlled velocity v4 failed L2 but validated that diagnosis:** morphology returned at 26.67 m RMS, while dense worm-like texture remained and v2's 24.77 m was not beaten. **v5 ablations resolved the artifact 2026-07-21 (local 4070 Ti, native CUDA):** v5a (resize+conv upsampling) left the worm texture unchanged — transposed conv falsified; v5b (Fourier timestep embedding replacing the broadcast time plane) removed it and set a new best **19.24 m RMS** (slope median 17.00 vs target 16.58, spectrum −3.39 vs −3.68, structure functions matching); v6 (both) tied v5b — **Fourier-time/transposed-conv is the architecture**. **Next:** remaining L2 gate evidence on the v5b contract (multi-step galleries, holdout gallery, memorisation check, residual stipple). Remaining: SLDEM distribution-rights confirmation and combined S0–S3 campaign | next | L–XL research | MIRA-0 schema | roadmap/mira_learned_terrain.md L1–L2 · mira_airless_mvp.md §6, §8 · ADR-20260721T033713Z-rust-native-learned-terrain · ADR-20260721T020849Z-local-cuda-first-mira-campaigns |
| MIRA-V0 | **Rebuild the stale Mira package:** checked-in key `4fed789a18a2ddda` is stale against expected `84094ad51a76cb37`, so every Mira runtime boot and headless capture fails. `just bake Mira` + `just validate-bake Mira`, then confirm `mira-orbit`/`mira-surface`/`mira-eva` capture. Prerequisite for the whole MIRA-V track and for clearing MIRA-MVP | done | S | — | rebuilt 2026-07-22 → key `3bc2b70c2ceaa46e` (neither the stale `4fed789a…` nor the recorded-expected `84094ad5…`; the key had moved again, so the note was a snapshot not a target). Validator clean; runtime-verified by four captures booting Mira ground terrain at radius 869 km · mira_airless_mvp.md §1 · ADR-20260722T084155Z |
| MIRA-V1 | **Reference-matched capture framings:** *(2026-07-22: disc + approach landed and inspected; `mira-disc` now aims at the parent so the near-side mare basin is presented, and a degenerate dark-side selection — flipping the perpendicular also flipped the derived up-axis, so both candidates put the sun on the same side — was fixed by flipping only the roll. Rim still blocked, see MIRA-V5/V6.)* deterministic `ScreenshotPreset`s for the three visual targets — orbital full disc (body fills frame, terminator visible), oblique low-sun approach (grazing light, long rim shadows), close crater rim (terraced wall + central peak at ~10–100 m detail). Fixed geometry so visual claims stop depending on ad-hoc cameras | verify | S | MIRA-V0; rim blocked on MIRA-V5/BL-33 | roadmap §"Visual targets" · ADR-20260722T084155Z · visual_testing.md |
| MIRA-V5 | **Landmark registry cannot serve "a large crater":** `BodySurfaceRegistry` filters craters to 4–30 km radius but then sorts by `abs(r − 10 km)` and truncates to 256, so the retained set is dominated by ~10 km features and the genuinely large ones are discarded before any preset sees them. `LandmarkChoice::Largest` inside the narrow `Grazing` band consequently resolved to a 6.0 km crater — too small to read as a rim. Fix: retain a size-stratified landmark set (keep the largest N alongside the typical N) rather than a single 10 km-centred truncation | verify | S | — | **Landed 2026-07-22:** `airless_landmarks` now keeps two concatenated sets — *typical* (young, ~10 km, first, so existing probes are byte-identical) and *most legible*, ranked by `radius x degradation_factor` using the renderer's own model (`degradation_factor` exported from `thalos_terrain` rather than duplicated). Two gates were starving the large end, not one: the `age <= 1 Gyr` filter excludes big craters almost by construction, and the surviving list was truncated around 10 km. Landmark radius went 6.0 km → 25.7 km. `mira-rim` also gained crater-scaled standoff (`landmark_radii`) and moved to 20° elevation / `Oblique` light. **The rim shot still does not read as a crater — see MIRA-V7, which this work uncovered.** Refs: `terrain_registry.rs` · MIRA-V1 rim framing |
| MIRA-V7 | **Large craters are far too shallow to read at survey scale.** `terrain_baker diagnose` now profiles across the highest-legibility landmark: r 25.7 km, age 0.32 Gyr, degradation retained 0.977 (i.e. essentially undegraded), record depth 639.9 m / rim 1133.6 m, **measured rim-to-floor 820.9 m across a 51 km-wide feature — a depth:diameter ratio of 1.6%**. Real complex craters run 4–10% (Copernicus 3.8 km / 93 km). At the rim preset's 74 km standoff that dip subtends <1°, which is why three successive framing fixes all still rendered open plain. The crater exists in the height field and is not degradation-limited; the authored depth law itself is the constraint. Fix belongs in the compatibility producer's crater profile (`crater_profile.rs` / `morphology_for_radius`), and must weigh gameplay + collision + whole-body look, so it wants a decision before a patch. **Root cause found and fixed 2026-07-22:** the *complex* branch of `crater_dimensions` used `depth = 196 * D^0.301`, i.e. the **simple**-crater `d/D = 0.196` pasted into the complex formula while keeping the complex exponent. Pike (1977) complex is `d = 1.044 D^0.301` km. The rim coefficient beside it (236, from `0.236 D^0.399` km) was always right, which is what made the mismatch visible. Fixed to 1044; peak-ring and multi-ring rescaled (1342 / 1785) for continuity at the D=137 km and D=300 km boundaries; simple→complex now blends over a transition band because a hard switch left a 2x depth cliff. Verified: Copernicus-scale D=93 km now 4.09 km vs ~3.8 km measured; the probe landmark went 639.9 m → 3624 m record depth and 820.9 m → 3736.9 m measured rim-to-floor (d/D 1.6% → 7.3%). `mira-approach` now shows a large crater with a lit rim and shadowed interior where it previously showed none. **Still open:** the `mira-rim` close framing does not yet compose — with the landmark now chosen by surviving relief it lands on a deep bowl and frames the smooth floor. Body height range ±5334 → ±6132 m, so collision/camera-floor margins want a check | verify | M | — | MIRA-V5 · `terrain_baker diagnose` landmark probe 2026-07-22 · roadmap §"Visual targets" |
| BL-34 | **`mira-eva` renders black.** The canonical eye-level probe returns an all-black frame with only a thin lit horizon rim; a bright body sits *below* the horizon line, suggesting the site is unlit. **Not caused by the BL-33 lighting-normal work** — matched captures with the shaders stashed to HEAD are byte-comparable black (`artifacts/visual/runs/bl33_eva/`). **Both benign explanations are falsified — this is a real render bug.** Instrumented 2026-07-22: the site logs **solar incidence 34.6° (lit)** — a high sun, not night; and `terrain_baker diagnose` reports camera clearance 0.71–114.38 m over 64 azimuths, so the eye is above the surface, not buried. Lit ground at eye level is rendering black while a thin horizon rim stays bright. Leading candidate is `terrain_self_shadow`'s horizon march (W12): at a 1.7 m eye height the marched horizon is extremely close, so a small bias/step error would shadow every fragment while leaving distant terrain that pokes above the march unaffected — which matches the bright-rim-over-black-foreground signature exactly. **ROOT CAUSE PINNED 2026-07-22 — it is a geometry/height-parity bug, not shading.** The self-shadow hypothesis was wrong on inspection (that march is per-fragment, so it would darken `mira-approach` identically; it does not). Two typed comparisons settle it: **`terrain-lighting`** shows *fullbright* rendering only sparse slivers rather than solid ground (MAE 1.28/255, 2.6% px) — the inverse of INC-0009, where fullbright proved complete opaque coverage — so there is almost no terrain geometry in front of the camera; and **`terrain-culling`** shows *two-sided* revealing large terrain swathes that backface culling hides (MAE 7.73/255, 7.8% px). **The EVA eye is inside the rendered mesh, seeing culled backfaces.** The eye is placed from the CPU `HeightSource` (`diagnose` reports 0.71 m minimum clearance) while the rendered mesh sits higher at that point, so a metre-scale CPU-vs-GPU height disagreement buries a 1.7 m eye. Scattered rather than uniform patches in the two-sided frame match a mesh weaving above and below eye height. This is the CPU/GPU height-parity tolerance MIRA-3's exit criteria already names — fix the parity, do not paper over it by raising the eye. Evidence: `artifacts/visual/runs/bl34_eva/`. **PARKED 2026-07-22 — superseded by the renderer replacement.** The udlod custom render path is being replaced with a Bevy/`big_space`-native renderer tailored to diffusion terrain, and this defect lives entirely inside udlod's tile/LOD machinery, so debugging it is work on a layer being deleted. **Keep the falsifications** — they are equally true of the new renderer and save re-deriving them: the cause is NOT package ancestor-fallback shear (rebaked at a 10 m prune budget, 0% pruned, max declared error 0.026 m, frame unchanged), NOT backface culling at grazing range (two-sided at 50 m eye height is MAE 0.08/255), and NOT `cull_behind_view` (notches survive with it disabled). The eye-height signature is the useful reproduction: 1.7 m black / 50 m serrated horizon / 2000 m clean, i.e. the failure scales with how grazing the view is. Re-open against the new renderer only if it reproduces. | later | S | — | MIRA-V7 · INC-0009 · `eva_surface_context` |
| BL-35 | **Mira terrain silhouette is torn — black slivers where faces should be** (user in-game frames 2026-07-22; Thalos never shows it). Ranked hypotheses, all Mira-specific: **(1)** package ancestor-fallback shear — the pyramid declares a **256 m fallback budget** with per-level max declared error 226/255/246 m, so adjacent tiles resolving at different levels can differ by ~250 m of height; at a grazing view that is a black gash along a seam, and Thalos cannot reproduce it because `ProceduralSurface` is continuous so neighbours agree exactly. **(2)** the global `approximate_height` scalar: `tile_tree.rs` samples *one* height under the camera per frame and `vertex.wgsl` uses it for every vertex's `approximate_view_distance`, which drives `compute_morph` — standing 3.6 km down a crater floor mis-estimates distance for all distant vertices, so neighbouring tiles morph to inconsistent LODs and crack. Mira's relative relief is 3x Thalos's (±6147 m/869 km = 0.71% vs ±7445 m/3186 km = 0.23%) and MIRA-V7 increased it. **(3)** backface culling is why the cracks read black, not why they exist (the `terrain-culling` compare fills them two-sided). **Very likely the same root cause as BL-34**, where the EVA eye sat inside the mesh at 0.71 m clearance — what a few-hundred-metre seam shear does to a 1.7 m eye. **Discriminator run 2026-07-22 — hypothesis (1) FALSIFIED.** Rebaked at a 10 m prune budget: 0% pruned at every level, max declared error 0.026–0.094 m (was 226–255 m), i.e. every ancestor fallback eliminated. The frame is unchanged. Package shear is not the cause; budget reverted to 256 m. **BL-34 and BL-35 are the same bug.** An eye-height bracket at the identical site is decisive: 1.7 m = entirely black, 50 m = renders but the horizon is visibly serrated with dark notches (reproducing the user's in-game frames), 2000 m = nearly clean. So terrain fails along the *silhouette* and the failure scales with how grazing the view is; at 1.7 m the whole frame is grazing, so everything tears out. Remaining candidates: per-tile bounding/frustum culling computed from a mean height (tiles whose terrain rises above the horizon get culled as below it), `cull_behind_view` on the surface tier, and the global `approximate_height` morph error. Next: pin `approximate_height` to the body mean, then disable `cull_behind_view`, capturing at 50 m where the notches are legible but the frame is not black. Evidence: `artifacts/visual/runs/bl34_eva/eye_{50,2000}m.png`. **PARKED 2026-07-22 — superseded by the renderer replacement.** The udlod custom render path is being replaced with a Bevy/`big_space`-native renderer tailored to diffusion terrain, and this defect lives entirely inside udlod's tile/LOD machinery, so debugging it is work on a layer being deleted. **Keep the falsifications** — they are equally true of the new renderer and save re-deriving them: the cause is NOT package ancestor-fallback shear (rebaked at a 10 m prune budget, 0% pruned, max declared error 0.026 m, frame unchanged), NOT backface culling at grazing range (two-sided at 50 m eye height is MAE 0.08/255), and NOT `cull_behind_view` (notches survive with it disabled). The eye-height signature is the useful reproduction: 1.7 m black / 50 m serrated horizon / 2000 m clean, i.e. the failure scales with how grazing the view is. Re-open against the new renderer only if it reproduces. | later | M | — | BL-34 · MIRA-V7 · `package.rs` fallback budget · `tile_tree.rs:824` · `vertex.wgsl:40` |
| MIRA-V6 | **Grazing light detonates the crater-compose aliasing at close range:** the `mira-rim` capture at 83° incidence / 30 km renders as a dense field of hard-edged dark blobs with no circular rim structure, far worse than the same site under `Oblique`. Consistent with INC-0009 (Hapke amplifying unresolved normal flips) and the same root cause as BL-33, but this is the first evidence it *blocks a framing outright* rather than being a survey-distance cosmetic defect. Close airless framings cannot use grazing light until it is fixed | next | M | BL-33 | INC-0009 · BL-33 · `artifacts/visual/latest/mira_rim.png` 2026-07-22 |
| MIRA-V2 | **Airless render calibration** at the MIRA-V1 framings: Hapke parameters, opposition surge, mare/highland albedo split, exposure and terminator response. Runs against the existing compatibility package — a bad result indicts the renderer, not the bake. Advances no milestone; informs L5 | verify | M | MIRA-V1 | **2026-07-22 slice landed:** Hapke `w` (single-scattering albedo) was pinned at 0.45 for every material while the caller applied albedo as a flat multiply, so mare and highland got *identical* multiple scattering (`H(mu0)H(mu)-1`) when it should collapse for dark material — the province contrast was only the linear albedo ratio, which tonemaps to near-invisible. `w` is now per-channel and material-driven (`hapke_w_from_albedo`, ×4.0 → highland 0.41 / mare 0.17 / fresh 0.94, matching the values this shader's own comment cites), the double albedo multiply is gone, and `HAPKE_OUTPUT_SCALE` restores the prior exposure so the change is contrast-only. Disc inspected: mare basin clearly separated, highland brightness held. `mira-orbit` unchanged (dominated by BL-33 speckle + flat 65° light). **Impostor blast radius checked 2026-07-22:** `shade_hapke_surface` is shared by `planet_impostor.wgsl` and `solid_planet.wgsl`, so every body impostor shades through it. Matched before/after `cloud-planet` captures (shader stashed for the baseline, same preset/world/seed) are visually indistinguishable — no blowout, colour shift, or terminator change; evidence in `artifacts/visual/runs/hapke_w_impostor/`. **Caveat:** that framing is cloud-dominated (clouds composite outside the Hapke path), so it is a weak test of the *land* path; a cloudless impostor framing would test it properly. Also removed the now-dead fixed-`w` `hapke_brdf` shim — nothing called it once `shade_hapke_surface` moved to the RGB entry point. **2026-07-22 second slice:** airless brightness lifted ~0.45 stop via `HAPKE_OUTPUT_SCALE` 0.128 → 0.175 (the one gain knob for the Hapke path; also reaches impostors, global exposure stays with GF-CAL), and inter-crater regolith smoothed — `AirlessRegolithParams` amplitude 50 → 15 m, base wavelength 700 → 1100 m. The old ±50 m over 700 m ran ~14% slopes with octaves to ~44 m and, being LOD-invariant, read as an all-over ripple at every range that competed with the craters; correcting the crater depth law (MIRA-V7) made that imbalance worse. Disc and approach both inspected: smooth between craters, craters read as bowls. Remaining V2 scope: opposition surge, terminator response. Refs: roadmap §"Visual targets" · gfx §3 · ADR-20260722T084155Z · `lighting.wgsl` |
| MIRA-V3 | **Crater ray systems:** the authored province vocabulary covers mare/highland and fresh/mature albedo but has no ballistic ejecta ray pattern — the single most recognisable feature of the orbital full-disc framing, and currently absent by omission rather than by design | verify | M | MIRA-V2 (calibration first, so rays are tuned against a correct albedo baseline) | ADR-20260722T084154Z · `assets/bodies/mira.ron` |
| MIRA-V4 | **Single-face 4096 hero bake** on the v5b checkpoint: real learned terrain in-game at campaign face resolution, one face, no cross-face seam/consensus machinery. Throwaway integration artifact — must not be checked in as a shippable package or counted as MIRA-2 progress | next | M | MIRA-V0; v5b checkpoint (have) | roadmap Track B · ADR-20260722T084155Z |
| MIRA-2m | **Material/province node kind in the package schema:** `PackageBlobKind` carries only `HeightBase`/`HeightResidual`/`Conditioning`; Mira's albedo rides the `StaticSurfaceV1` compatibility substrate that MIRA-2 replaces, and the diffusion producer emits height only. Add the explicit province node kind (authored/procedural field, seed-conditioned, CPU-reproducible) **before** the first whole-sphere campaign, or the bake renders grey | next | M | ADR-20260722T084154Z accepted | mira_airless_mvp.md §5, §8 MIRA-2 · `package.rs` `PackageBlobKind` |
| BL-33 | **Airless crater speckle — FIXED (lighting normals; height aliasing ruled out by measurement):** *(Renumbered from a colliding BL-24 — Track 3's perspective-handoff row was minted concurrently and is `wip`; this row kept no external references.)* sub-bake-threshold craters composed LOD-invariantly alias into dark ragged specks when a whole crater falls below one tile texel (visible in `mira-surface` at 46 km after the 2026-07-21 merge removed the f32-acos rim sawteeth). Next candidates, in order: (1) the shader-side airless normal/albedo path under grazing incidence — INC-0009 fixed the same symptom class there and the `mira-rim` grazing capture is far worse than the oblique one at the same range, which is a lighting-response signature not a geometry one; (2) the height-atlas relief normal (derivative/mip selection) as distinct from the height values themselves, which the audit shows are fine; (3) the Hapke response itself at high incidence. The LOD-invariance rule is **not** in tension after all — no ADR needed unless a later candidate reopens it | verify | M | — | feature_compositor.rs · INC-0009 · mira-surface probe · **2026-07-22: reproduced far more severely by `mira-disc`/`mira-approach`/`mira-rim`; MIRA-V6 shows it blocks the close crater framing outright.** **Height-aliasing hypothesis FALSIFIED by measurement** — `terrain_baker diagnose` gained an alias audit (point sample at texel spacing vs box mean of the same span at 1 m). Baseline residual is only 0.148 m @ 25 m texel and 5.0 m @ 400 m against 107 m of within-texel relief, i.e. <5%: the height field is already effectively band-limited, because within-texel relief is dominated by the smooth slope across it and a midpoint sample of a ramp equals its mean. Three candidate fixes (footprint-faded crater compose; threshold-faded fBm octaves; sinc box-transfer fBm octaves) each made the residual 5–10× **worse** and were reverted. Search redirects to the **shading** path — normals/albedo — matching INC-0009, whose identical symptom class was also not geometry and was fixed shader-side. **ROOT CAUSE FOUND AND FIXED 2026-07-22 — it was the lighting normals, three compounding attenuations of resolved macro relief:** (1) `HEIGHT_NORMAL_WEIGHT` 0.35 applied only a third of the height atlas's normal; (2) `stable_normal` pulled another 15% back to the sphere; (3) `shade_hapke_surface` clamped n·l against **body curvature** plus a headroom that collapses to its 0.05 floor below `geo_n_dot_l` 0.15 — at 83° incidence that caps n·l near 0.17, so a crater wall tilted 30° sunward (true n·l ~0.60) shaded 3.5x too dark. Crater interiors could never exceed body-average brightness whatever their walls did: flat dark patches instead of bowls with a lit far wall, worst at grazing. These were blunt mitigations for INC-0009 that suppressed the signal rather than filtering it; its real fix (footprint-filtered octaves) has since landed. Now: weight 0.85, and `ThalosSurface` carries a separate `relief_normal_ws` so the micro-facet headroom references *resolved terrain* while `geo_normal_ws` stays sphere-outward for the grazing-view visibility floor — conflating the two blacked out the entire eye-level frame, which is exactly what the existing comment warned about. `mira-approach` verified: craters read as bowls with lit far walls |
| MIRA-2 | **Whole-sphere adaptive Mira bake:** 4096-face campaign starting point, sphere-native priors + direction seed, cross-face tangent windows/overgenerated seam consensus, canonical package borders, hierarchical diffusion, H0 occupancy/material channels, complexity/error-driven residual pruning and per-node codec selection; fixed Mira package + limb/face/equirect/rate-distortion/SFD/seam evidence | later | XL | MIRA-1; **MIRA-2m** (material node kind, else the bake renders grey) | mira_airless_mvp.md §5–§6, §8–§9 · ADR-20260722T084154Z |
| MIRA-3 | **Client reconstruction + cache:** deterministic conditioned close-band terrain, collidable parity boundary, cosmetic micro + airless boulders; package/decode/reconstruction cache layers and cold/warm benchmarks; evaluate an optional small single-pass residual CNN only if procedural detail fails the low-approach gate, with Tier 0 independent of learned weights | later | L | MIRA-2 | mira_airless_mvp.md §5, §7–§9 |
| MIRA-4 | **Mira playability + acceptance:** parameterised target-body orbit/EVA routes, headless map/orbit/approach/surface matrix, height-authority evidence, tuning, and user play-session checklist | later | M | MIRA-3 | mira_airless_mvp.md §8–§9 |
| F4r | F4 remainder: SH-9 + spine (terrain) ambient port; env-cubemap crossfade; space ambient retires with F7 | next | M | — | gfx §3 F4 |
| F5r | AO onto veg/rock/grass/hull + `StandardMaterial` ambient; spatial blur; VBAO upgrade (W8) | next | M | — | gfx §3 F5 · §4.3 |
| W12r | Terrain horizon self-shadow, object-side v2: per-fragment horizon term for spine materials (trees/grass/rocks), longer-than-resident reach, max-mip acceleration. **The far-field tier** of the three-tier shadow split | next | M–L | — | gfx §4.2 · ADR-20260722T111848Z-shadows-three-tier-not-virtual-shadow-maps |
| W18 | **Contact + mid-field shadow tiers** (pulled from `GF-pool`). **a:** screen-space contact shadows — short march in `SceneDepthImage`, ~8–16 steps, sub-metre reach, reusing F5's SSAO node pattern; grounds craft/gear/trunk bases and fixes peter-panning. **b:** `cascade_factor` onto a hardware `sampler_comparison` (16 `textureLoad`s + hand-weighted tent → 9 `textureSampleCompare` taps at equivalent filter quality) — a prerequisite that funds **c:** PCSS contact-hardening on cascades 0/1 + a cross-cascade blend (only the outermost cascade fades today; 0→1 and 1→2 hand off hard). Cascade extents deliberately do **not** grow — far field is W12r's job. **a landed 2026-07-22** (workspace check + clippy clean): new `rendering::contact_shadow` full-res R16Float pass over `SceneDepthImage` (full-res, not SSAO's half — a contact shadow is high-frequency and its casters are a few px wide; no blur, jittered march instead), 12-step view-space march toward the sun with a thickness test so background geometry can't shadow the foreground, grazing-widened normal bias, distance fade-out at 120 m. Gate published in `ShadowCascadeBlock.gate.z` by the rig (not a per-material flag) so every consumer inherits it; `thalos::shadow::contact_shadow_factor` is the shared sampler. **Terrain is the only receiver so far** — same staging F5 used. `THALOS_CONTACT_SHADOW=off/show` | verify | M | **b/c still next.** Follow-ons: veg/rock/grass + hull + `ShadowedStandardMaterial` (runway/structures) receivers — the runway is the highest-value one left, since parked-craft grounding is the point | gfx §4.2 · ADR-20260722T111848Z-shadows-three-tier-not-virtual-shadow-maps |
| W11 | **Hillaire aerial-perspective froxel volume** (pulled from `GF-pool`). 32³ RGBA16F view-space LUT over the in-atmosphere near range only (far plane = `min(atmosphere exit, ~128 km)`); the `BodySky` raymarch stays authoritative beyond it and above the Kármán line. Every opaque surface applies `color·T + L`, which **deletes `object_aerial_recession` + its 4 exposure-coupled constants** and closes the two-aerial-model seam (terrain recedes through the raymarch, the 5 vegetation callers through a distance lerp with no transmittance, phase, or altitude term). **b:** sample `thalos::shadow` per froxel → volumetric shafts, near-free once the march exists | blocked | L | **ships inside F7's slice** (the one shared view-level scene/atmosphere bind group) — wiring 8 materials before F7 means rewiring all 8 after; F7 in turn gates on GF-CAL settling F3/F4 + exposure, since the volume changes apparent brightness at distance | gfx §4.5 · ADR-20260722T111847Z-aerial-perspective-froxel-volume-regime-split |
| BL-37 | **`aerial` + `shadow` comparison axes** — the first concrete instances of BL-19's debug-channel program, and the acceptance gate for W11/W18. `aerial`: off / analytic / froxel across `THALOS_SCREENSHOT_DISTANCE` brackets **and an ascent through the Kármán gate** (both W11 handoffs are new artifact surfaces). `shadow`: cascade-only / +contact / +horizon / cascade-index false-colour. Neither item is verifiable from a single beauty frame — the froxel volume in particular will look plausible while being energy-wrong. **`shadow` axis landed 2026-07-22** with W18a (`cascade-only` / `contact` / `raw`, env `THALOS_CONTACT_SHADOW`, wired through `apply_live_capture_diagnostics`); `aerial` axis waits on W11 | wip | S | BL-18 (harness exists); `aerial` half blocked with W11 | visual_testing.md · BL-19 · ADR-20260722T111847Z · ADR-20260722T111848Z |
| W1 | Aggregate canopy/grass colour baked into terrain albedo (kills the orbit→ground pop; tint-out and geometry-in as one coupled curve) | next | S | — | gfx §4.7 |
| W2 | Cloud sun transmittance for all receivers — retained stable ID, now subsumed by CLOUD-5: global density-derived tail + near cascades, also drives atmosphere shafts and overcast ambient | later | L | CLOUD-4; cloud architecture decision | gfx §4.2 · cloud §3.5 |
| W3 | GoT grass shader tricks: view-space blade widening + curved normals + fractional-width LOD | next | S | — | gfx §4.7 |
| W4 | Two-sided foliage translucency + bake into the impostor; pull W21 (foliage atlas mips) forward with it | next | S | — | gfx §4.7 |
| C1 | Tonemapper A/B: AgX vs Khronos PBR Neutral (ACES rejected) | next | S | after GF-CAL settles exposure | gfx §4.9 · Open Q2 |
| VEG-R | One `VegLayer` driver folding `grass.rs`/`vegetation.rs`/`rocks.rs` (they triplicate the clipmap lifecycle, diverge on base-clearing) | next | M | Open Q5 call | gfx §4.7 |
| BL-1 | GPU grass slice 2: compute cull + indirect draw (slice 1 landed behind `GraphicsSettings::gpu_grass`, preview-verified) | next | M | — | vegetation.md |
| CLOUD-0 | Planet-cloud baseline, probes, and budgets: cloud-specific headless runway/cruise/interior/limb/sunset presets; current 1080p/1440p GPU+memory baseline; artifact inventory and acceptance captures | done | S–M | agent probes landed; user identified overall fidelity as the blocker and supplied the Blackrack/KSP visual bar | cloud §4 CLOUD-0 · cloud_baseline.md |
| CLOUD-1 | Canonical cloud ownership/schema: one authored `CloudClimate`, one per-body runtime `CloudWeatherField`, `None` authoritative, first weather-derived orbit layer; absorb render mechanism under `body_render` and delete legacy/reference ownership | done | M–L | compile + warning-clean; all five headless cloud regimes captured without asset/WGSL/pipeline failure 2026-07-20 | ADR-20260720T212214Z-one-weather-field-many-cloud-projections · cloud §2–§4 |
| CLOUD-2 | Scalable targets and temporal reconstruction: viewport-relative/interleaved march, history rejection/clamp/upscale, screenshot mode and quality ladder | done | L | completed 2026-07-21 and motion-corrected by BL-29: viewport-relative 1/2–1× targets; rotating 3×3 sparse march only while screen-static; full current-ray march plus body-fixed, hit-aware history stabilization during camera motion; cut/depth/epoch rejection, 3×3 neighborhood clamp and hit-aware upscale. 1440p High sunset 3.35 ms mean / 3.36 p95 at 1712×960 (≤3.5 ms target) | cloud §3.3 · §4 · cloud_baseline.md · INC-0016 |
| CLOUD-3 | Multi-scale density and range: typed vertical profiles, true 3-D base/detail noise, anti-tiling, cadence-safe range LOD | done | L | corrected 2026-07-21 after matched A/B: heuristic hierarchy leaps caused posterized height strata and were deleted (ADR-20260721T033055Z-cloud-skips-require-conservative-bounds); BL-33 refinement 2026-07-22 keeps that invariant with 600 m broad-mass probes, monotonic 120 m full-density refinement, footprint-filtered erosion, and 112 probes / 67.2 km clear reach with a 25 km-bounded context anchor. Five cold views clean; 1080p Baseline worst accepted mean 1.474 ms runway / 1.229 ms sunset | cloud §3.2–§3.3 · INC-0010 · INC-0011 · cloud_baseline.md |
| CLOUD-4 | Atmosphere-coupled cloud lighting and correct foreground/background media ordering | wip | L | 2026-07-22 lighting-completion slice headless-verified: filtered multi-tap sun optical depth (smooth broad mass only, jittered ladder) drives per-octave multi-scatter attenuation; restrained powder; rebalanced ambient; CPU low-sun chromaticity no longer double-reddens over the per-sample air-mass term; foreground airlight restored analytically in both near march and far overlay (`AIRLIGHT_GAIN` / composite veil) — sunset probe shows backlit grey-blue masses, no soot/mud/strata. Residual: bind the shared Thalos sky/transmittance LUT explicitly in place of the analytic β estimates | cloud §3.4 |
| CLOUD-5 | One-world cloud sun transmittance, matched godrays, surface receivers and overcast environment response | later | L | CLOUD-4 + F6 substrate | cloud §3.5 |
| CLOUD-6 | Density-derived orbital projection and invisible surface/orbit/map handoff | wip | M–L | second slice 2026-07-22 headless-verified; BL-33 raised the canonical weather cube to 1024² faces / 8 mips (~4.9 km base texels), enriched its shared mesoscale/cellular hierarchy, cold-verified a seam-free disc, and moved the partition handoff to 67.2 km (`march_reach` stays in lockstep). The 6-sample analytic weather-column band march remains visibly slab-like at limb/cruise/sunset. A 24-sample live march and multiple footprint-filtered four-stratum atlas producers were fully reverted. Single-tile, weather-phase-warped, and guaranteed incommensurate Cartesian sources all retained a curved planet comb: the spherical shell cuts coherent repeated 3-D surfaces into bands. The far producer must be surface-parameterized (body direction + normalized height) and shared into near density, then emit OD/albedo/normal/height moments plus a reduced-detail limb representation; motion handoff verify (**user**) | cloud §3.6 · ADR-20260722T111036Z · ADR-20260722T135123Z · ADR-20260722T141000Z |
| CLOUD-7 | Simulation-time weather advection/growth, fronts/cyclones and authoring/debug tools | later | L | CLOUD-5 + CLOUD-6 | cloud §3.1 · §4 |
| CLOUD-8 | Inside-volume extinction, precipitation/rain shafts, lightning path and final quality polish | later | M–L | CLOUD-4 + CLOUD-7 | cloud §4 |
| BL-33 | **Planet-scale cloud fidelity convergence:** accepted slices: 1024²/8-mip canonical weather with richer shared mesoscale/cellular structure; monotonic 600 m broad → 120 m full-density adaptive march with footprint-filtered erosion; 112 probes / 67.2 km clear reach and 75 km shell cap with a bounded context anchor; continuous weather phase offsets on the broad + formation domains (near runway/cruise gates clean). Pure proportional cadence was rejected after cold A/B; runway contour-extrema improved 19.6% before the later phase slice. Five final cold probes clean and under budget. Direct exact-density far marching and Cartesian-density atlases (periodic, phase-warped, and incommensurate dual-domain) were all rejected/reverted. Residual: a shared surface-parameterized density seed/contract, then an optical/albedo/normal/height atlas + reduced-detail limb representation; further reach only with budget/true bound. | wip | XL | CLOUD-3; CLOUD-6 second slice | cloud BL-33 checkpoint · cloud_baseline.md · ADR-20260720T212214Z · ADR-20260721T033055Z · ADR-20260722T102639Z · ADR-20260722T111036Z · ADR-20260722T135123Z · ADR-20260722T141000Z |
| TM1 | Tiling-material detail: material-ID height-biased weight blend over a texgen material array | later | M | mind the Metal bind budget (F7 note) | gfx §4.6 |
| TM3 | Collapse the palette/mirror debt onto the `SurfaceQuery` seam — **largely overlaps TM-P1; re-scope against what Phase 1 landed before starting** | later | M | TM-P1 verified | gfx §4.6 · terrain_macro.md |
| F7 | Metallic branch in `surface_brdf` + one shared view-level scene/atmosphere bind group + prefiltered env from the F3 LUT. **Now also carries W11's froxel volume** — the shared view-level bind group is what the volume binds through, so the two ship as one slice | later | L | F3/F4 verified — i.e. GF-CAL is the real gate; F7 then unblocks W11 | gfx §3 · ADR-20260722T111847Z-aerial-perspective-froxel-volume-regime-split |
| F8a | Structures onto `shade_surface` | later | M | F7 | gfx §3 |
| F8b | Hull onto `shade_surface`; retire the CPU reflection probe + magic constants | later | M–L | F8a | gfx §3 |
| F9 | `FOLIAGE`/`WATER` branches in `shade_surface`; retire `shade_foliage` + the parallel `thalos::water` BRDF wrappers while preserving analytic `BodyOceanMaterial` ownership (the dormant mesh-water path is already deleted) | later | M | F7 | gfx §3 · ADR-20260720T185954Z-analytic-planet-water-never-meshed |
| W13 | **Whole-scene temporal reconstruction foundation:** one body-fixed per-view contract for depth/motion vectors, jitter, exposure, cut/reset epoch, render/output extents, and UI ordering; prove it with native-resolution temporal resolve while preserving SMAA fallback | later | L | after current unification/calibration pass; establish a 4K GPU baseline | gfx §4.8 · ADR-20260721T042905Z-temporal-upscaling-with-optional-dlss |
| W13-DLSS | **Optional DLSS Super Resolution / DLAA projection of W13:** Vulkan/RTX runtime detection, feature-gated `dlss_wgpu` v4 integration for `wgpu 29`, quality settings, packaging/license path, and native fallback; no Frame Generation or Ray Reconstruction in this slice | later | M | W13; measured GPU-bound win at 4K | gfx §4.8 · ADR-20260721T042905Z-temporal-upscaling-with-optional-dlss |
| GF-pool | Later-sprint pool — W14–W17, W20, TM4, 6b, SSR, bent normals… stays at doc granularity until pulled in. *(W11 + W18 pulled in as rows 2026-07-22.)* | later | — | — | gfx §4 |
| BL-4 | Orbital black continents + coast speckle: sky-pass near/far classification → shell-segment membership; shelf ease-out shoulder; awash-reef fold (offshore relief never breaches — `GENERATOR_VERSION` 5); error-aware shoreline tie-band feather | done | — | — | INC-0003 · verified via `THALOS_SCREENSHOT_DISTANCE` bracket captures 2026-07-19 |
| BL-7 | Distant terrain grey/shiny/pixelated: material-mask bake divided its slope/curvature stencil by a step clamped to 250 m while coarse texels span km — rock+wetness masks saturated at every LOD coarser than 250 m/texel (fix: true `tile_lod_m` step + fractal-scale rock-slope compensation `(step/30 m)^0.35`; `GENERATOR_VERSION` 8) | done | — | — | INC-0004 · verified via `THALOS_SCREENSHOT_DISTANCE` brackets 8/100/400/1800 km 2026-07-20 |
| BL-8 | **Coastline as authored data** (ADR-20260720T185957Z-coastline-as-authored-data): per-body coast/bathymetry cube baked from the `SurfaceQuery` surface at spawn; `BodySky` ocean crossfades authority by range — near = depth-compare (exact), far = atlas coverage/colour + height-based occlusion. Round 2 (user live-eye findings): foreshore deepened 4→15 m so archipelago seas stop reading as one vast translucent bank; awash reef 2→6 m (surface "scum" rings → pale submerged bathymetry); shallow-colour e-folding 14→8 m; near-feather error floor models mesh coastal sampling error (2e-5·t, kills mid-range coast "bites" + tile-seam leak-through); atlas 512→1024 so macro islands stay resolved at range (`GENERATOR_VERSION` 10) — **user fly-over failed verification 2026-07-20** (speckle + translucent wash regenerated); the depth-compare/crossfade half is superseded by BL-9 / ADR-20260720T185958Z-water-projects-one-signed-sea-field, the coast atlas survives as the cascade tail | done | — | — | ADR-20260720T185957Z-coastline-as-authored-data · ADR-20260720T185958Z-water-projects-one-signed-sea-field · INC-0003 |
| BL-9 | **Water as a projection of the one signed sea field** (ADR-20260720T185958Z-water-projects-one-signed-sea-field): the analytic ocean coverage/colour samples directly from the resident udlod height-tile atlas (tile-tree walk at footprint LOD, mip-sampled) with the coast atlas as the coarse tail — same field, two resolutions, no authority crossfade; depth demoted to resolvable-geometry occlusion (field-assisted land test + footprint-scaled in-front margin for craft). Deletes the `2e-5·t` mesh-error feather, the error-aware tie band, `OCEAN_AUTHORITY_*`, and the reconstructed-height occlusion thresholds. `BodySkyMaterial` owns the canonical bind implementation and `BodyOceanMaterial` delegates to it | done | — | — | ADR-20260720T185958Z-water-projects-one-signed-sea-field · body_sky.wgsl · sky_material.rs · screenshot brackets + **user fly-over verified stable 2026-07-20** |
| BL-10 | **Crisp beaches + shore-water interaction** (user directive 2026-07-20: no marshy semi-island coasts; beach biome now, other coastline types later). Phase A generator/landcover: coast-band detail-meander suppression (clean waterline arcs; keep the BL-6 domain warp; awash shoals pushed offshore), beach berm+foreshore profile, sand material band + veg/grass clearing, wet-sand darkening (0..~0.7 m), `GENERATOR_VERSION` bump. Phase B water optics: steeper shallow opacity (sand visible only ≤ ~1.5 m column — kills the soupy translucent shelf + tile-seam show-through), shallow tint from seabed material. Phase C tier 1 (MSFS-class, all normals/albedo in `shade_ocean` — ADR-20260720T185954Z-analytic-planet-water-never-meshed intact): shoreline + breaker foam bands from the signed field, wave shoaling (amplitude/λ shrink with depth) + refraction (align to −∇h); tier 2 later = advected breaker wavefronts (Sea-of-Thieves-class) | verify | M–L | user picked A+B+C1 2026-07-20; landed same day, 3 generator rounds (`GENERATOR_VERSION` 17): beach berm +4 m, thresholded islet clumps, shore band 60→9 m, **offshore shallow clearance** (relief may not park seabed in (−14 m, 0) away from the macro foreshore — the structural island-halo kill; deepening the awash fold alone did nothing, the halo was legal non-breaching tops), coverage AA band now uses the MEASURED local slope from the gradient taps (fixed-slope bands painted flat shoals as 40 % land at coarse footprints); sand strand + wet band in `eval_material_stack`; veg cleared ≤ +4 m (`VEG_BEACH_CLEAR_M`); turquoise shallows; shoaling + refracted breaker/swash foam in `shade_ocean`. Brackets clean (20 Mm/4 Mm/1.8 Mm/500 km); probes: 0 m wander × 8, 0 % breach, land 35.3 %, runway dry. **user:** beach walk + low flight over coast/archipelago | ADR-20260720T185958Z-water-projects-one-signed-sea-field · terrain_macro.md · water.wgsl |
| BL-12 | **Open-ocean fidelity vertical slice:** dedicated low-sun/deep-water headless preset; world-anchored directional multiscale slopes; sky/sun Fresnel response and horizon-energy preservation inside ADR-20260720T185954Z-analytic-planet-water-never-meshed's analytic-sphere path. Reopened/fixed 2026-07-21 after user review: direct scalar-gradient normals produced closed “worms,” and one isotropic grazing footprint erased all directional detail. Replaced with one shared 256² dual-spectrum slope texture sampled across four physical scales; full mip chain + explicit major/minor `textureSampleGrad` + 16× anisotropy preserve cross-wave detail to the horizon, and mip-omitted variance transfers into GGX. Sparse foam is gated from exceptional resolved slopes. Headless-verified in `ocean_final.png`. | done | M | — | ocean.md · INC-0012 · ADR-20260720T185954Z-analytic-planet-water-never-meshed · `just screenshot ocean` |
| OCEAN-1 | **First measured spectral tracer:** one validated per-body `OceanState`; existing packed slope texture split into 128-carrier low/high frequency packets; both packets evolve per cascade at representative deep-water dispersion rates with f64 CPU phase reduction tied to canonical simulation time; `ocean-slopes` headless diagnostic separates field topology from BRDF response. Phase-0/45 production + diagnostic captures inspected; horizon/foreground detail preserved, diagnostic RMSE 0.024 across phase pair. No FFT, displacement, persistent foam, SSR, wake, or shallow solver in this slice. | done | M | BL-12 | ocean.md §6 · ADR-20260720T185954Z-analytic-planet-water-never-meshed · ADR-20260720T185958Z-water-projects-one-signed-sea-field · `ocean_t45.png` |
| BL-26 | **Atmosphere-independent analytic ocean:** the former Bevy-atmosphere suppressor hid `BodySky` and therefore deleted the ocean. One dedicated `BodyOceanMaterial` now owns the signed-field/spectral projection independently of atmosphere visibility; `BodySkyMaterial` is atmosphere-only, with explicit BodySky → ocean → cloud transparent ordering and one shared binding/shader implementation | done | M | verified 2026-07-21: `ocean` restores the spectral water/sun road and `ocean-slopes` displays the independent slope/variance field | ocean.md · ADR-20260721T050036Z-ocean-composite-independent-of-atmosphere · INC-0013 |
| BL-27 | **Restore the authored physical Bevy atmosphere column:** the temporary `0.1` density adapter erased low-sun Rayleigh/Mie optical depth, leaving the canonical 1° sunset navy and its LUT-lit clouds white. Density is restored to `1.0`; brightness reconciliation moves to F7 light/exposure units. The Bevy sky-view LUT now supplies cloud ambient chromaticity only, retaining the calibrated cloud-energy scale | done | S | verified 2026-07-21: matched `cloud-sunset / atmosphere` improved from 45.88 to 15.55 MAE/255; final Bevy sunset, ocean, slope diagnostic, and runway-atmosphere frames are clean | atmosphere.md · INC-0014 |
| BL-28 | **Live atmosphere-backend switch crash:** removed the invalid persisted/live renderer choice. BL-30 subsequently deleted the remaining capture-only backend choice as well; stale `legacy_body_sky` keys remain load-compatible | done | S | focused migration test passed 2026-07-21; superseded runtime architecture covered by BL-30 | atmosphere.md · visual_testing.md · INC-0015 |
| BL-30 | **Restore the custom rocky atmosphere as the sole renderer:** matched runway/orbit evidence and live play exposed Bevy's camera-local proxy as washed-out, incomplete, and distance-unstable. The Bevy projection, backend override/comparison axis, cloud dependence on Bevy LUT resources, and terrain atmosphere-layout variant are deleted; `BodySky` is again the one atmosphere authority | verify | M | agent-verified 2026-07-21: check-clean; `runway-atmosphere`, `earth-reference`, and `ocean` captures inspected without pipeline/layer failure. **User:** confirm atmosphere stays attached through live surface→orbit motion | atmosphere.md · gfx §2.3/§4.5 · ADR-20260721T185221Z-custom-rocky-atmosphere |
| BL-29 | **Cloud temporal motion smear:** deterministic near-sun moving-camera probe; separate raw, dense-history, and production reconstruction; keep 3×3 amortization screen-static and require a fresh raymarch for every moving-view pixel, with coherent hit-aware history used only to stabilize that current result | verify | M | **user:** run `just game runway`, pan across the sun/cloud deck, and confirm no rectangular trails or delayed fill. Headless 1920×1080 motion matrix is clean; production p95 2.73 ms (≤3.5 ms budget) | cloud §3.3 · cloud_baseline.md · INC-0016 · user live capture 2026-07-21 |
| OCEAN-PROG | **Production ocean program:** JONSWAP/TMA FFT cascades producing displacement/slope/Jacobian behind OCEAN-1's authored `OceanState`; camera-relative local displacement compatible with ADR-20260720T185954Z-analytic-planet-water-never-meshed; persistent advected foam; bounded coast/wake coupling; explicit reflection hierarchy and GPU timing/debug views. BL-12/OCEAN-1 are filtered-slope visual tracers, not substitutes for these fields. | later | XL | F7/F9; profile before selecting grids | ocean.md §6 |
| BL-5 | Limb dash streaks — **root-caused and fixed 2026-07-20** after two disproven premises (not mesh-silhouette pokes: flatten-all-terrain left them identical; not lagoon geography: a whole-planet fragmentation scan found zero lagoon fields). Magenta-debug traced them to the ocean branch's fixed occlusion thresholds letting the coarse mesh's broken sliver raster of beyond-horizon coasts punch through the mask coverage at high anisotropy. Fixed: coast atlas carries a mip chain sampled at an analytic footprint LOD, coverage band scales with footprint, and occlusion thresholds scale with footprint (geometry only overrides the mask when resolvable). Verified at 20 Mm: dense dash bands → faint sparse fringe; mainland coasts clean. Plus a deep-tau aerial-veil extension (physically-motivated limb haze) | done | — | — | INC-0003 · body_sky.wgsl · proc_impostor.rs |
| BL-34 | **Tile-native surface seam:** make the multi-channel mipped tile the surface authority and derive `SurfaceQuery` point queries from materialised tiles instead of the reverse. Removes the raster→point→raster round trip on package-backed bodies (`PackageSurface` → `sample` per tile *pixel* → `compute_tile_pixels`), collapses the two hierarchies (package residual pyramid vs UDLOD quadtree) currently communicating through a scalar `lod_m`, and closes the `HeightSourceRegistry` CPU-fallback leak. Contract must make "a tile the bake did not anticipate" first-class: package hit → ancestor upsample → client synthesis → cache. Independent of how tiles are *drawn*, so it pays off under either outcome of the deferred renderer fork | next | L | — | ADR-20260722T105147Z-tile-native-surface-seam · terrain.md · terrain_lod_optimization.md |
| BL-35 | **Mira chunked-mesh / `ExtendedMaterial` terrain prototype:** replace UDLOD's shared-grid + height-atlas vertex displacement with per-tile `Mesh` assets under `ExtendedMaterial<StandardMaterial, MiraTerrainExt>` + CDLOD geomorph (morph factor = residual blend, exactly the package pyramid's own structure). Puts terrain on Bevy's default render path — same pattern `ShadowedStandardMaterial` already uses for crafts. **Mira only:** airless, Hapke-only, no atmosphere/ocean/vegetation/flatten pads/structures; `mira-orbit` / `mira-surface` / `mira-eva` are the harness. Thalos follows only if the slice holds | later | XL | **gated:** package schema frozen + one body producing real tiles from it, AND the `verify` queue burned down (attribution) | ADR-20260722T105147Z-tile-native-surface-seam · mira_airless_mvp.md |
| BL-36 | **Delete the unconditioned detail-noise field:** the 4 km-wrapped f32 shader noise has no orbital counterpart, so it adds content on approach rather than bandwidth — the current violator of the scale-consistency invariant ("refinement adds bandwidth, not a new skyline"). Replace with `Rclient`'s conditional refinement; delete, do not tune | later | M | MIRA `Rclient` band exists (mira_airless_mvp.md §7) | ADR-20260722T105147Z-tile-native-surface-seam · terrain_macro.md scale-ownership rule |
| BL-6 | ~~Coastal lagoon-swarm geography~~ **premise corrected, rescoped, landed 2026-07-20**: fragmentation scan found NO lagoon fields (the "lagoon" impressions were rendering artifacts, since fixed) — the real problem was bland uniform coasts. Landed: coast-character refinement in `continentalness` (`GENERATOR_VERSION` 11) — a ruggedness-modulated fractal domain warp (15→85 km displacement) + rugged-coast islet sprinkle, cost-gated to the coastal band; depositional regions keep sweeping beach arcs, erosional regions get headlands/coves/rias/archipelagos. world_map-verified (regional variety, land fraction held 39.3%, runway site dry, spaceport coast calm); coastline_lod probe: waterline still moves 0 m across LODs | verify | M | **user:** live-eye pass over varied coasts (beach + rugged) at flight altitudes | procedural.rs `continentalness` · world_map.rs |

## Track 3 — Meta / infrastructure

| ID | Item | Status | Est | Deps | Refs |
|----|------|--------|-----|------|------|
| BL-20260722T170654Z-documentation-taxonomy | **Reorganize the documentation tree by role:** keep only the canonical index, backlog, architecture, and gameplay vision at `docs/` root; group roadmap, gameplay-system, simulation, world, rendering, development, and reference docs into named folders; repair cross-references and record the taxonomy | done | S | — | docs/README.md · ADR-20260722T170654Z-documentation-taxonomy · link/path audit clean 2026-07-22 |
| BL-2 | Adopt the steering harness: this backlog + `steer` skill + ADR log + incident log + docs README | done | — | — | ADR-20260720T185953Z-steering-harness · 2026-07-18 |
| BL-3 | Polar orbit scenario (`just game polar` / start-screen + destruction picker) — same 200 km parking altitude as `orbit`, inclination 90° | verify | S | — | **user:** `just game polar`, confirm map/navball show polar path + ground track over poles | spawn.rs · debug_orbits.rs · boot.md |
| BL-17 | Codify headless screenshot + controlled A/B diagnosis as the required workflow for graphical fixes and visual iteration | done | XS | — | CLAUDE.md · documentation verified 2026-07-21 |
| BL-21 | Expand the CLAUDE visual-testing rule into an operational playbook: tool-selection boundary, controlled-axis procedure, artifact inspection order, failure caveats, and non-use cases | done | XS | — | CLAUDE.md · visual_testing.md · documentation verified 2026-07-21 |
| BL-22 | **Clean generated-artifact layout:** keep `artifacts/visual/latest/` to the latest canonical preset views; route comparison/iteration captures through `artifacts/visual/runs/`; route JSONL runtime diagnostics through `artifacts/diagnostics/`; prune superseded screenshot iterations | done | S | — | canonical output contract migrated and legacy local output trees pruned 2026-07-21 · tooling.md · visual_testing.md |
| BL-24 | **Player-to-agent saved perspective handoff:** F8 records the active 3D `ShipCamera` as a versioned body-fixed pose (position, orientation, lens, viewport, body, and boot scene) at `artifacts/diagnostics/latest_perspective.json`; `just screenshot latest` replays it through the real headless camera and writes the canonical latest view for agent inspection | wip | S | — | visual_testing.md · screenshot.rs |
| BL-27 | **Visible capture feedback:** F2 desktop screenshots and F8 perspective handoffs acknowledge saved/error outcomes with toasts, except in F1 photo mode; photo mode hides the shared toast container so capture frames stay clean | wip | XS | — | visual_testing.md · screenshot.rs · photo_mode.rs |
| BL-23 | Replace branch-conflicting sequential ADR numbers and hand-maintained index with timestamp-ordered distributed identifiers; migrate all existing ADRs and references | done | XS | — | ADR-20260721T034338Z-distributed-chronological-identifiers · documentation verified 2026-07-21 |
| BL-31 | **Persistent hot-patched visual iteration:** make `just screenshot` and `just compare` clients of one reusable headless game process; hot-patch Rust systems with Bevy/Subsecond, reload file-backed and embedded WGSL sources in place, reset capture histories between requests, and retain an explicit cold-process verification path | wip | L | — | tooling.md · visual_testing.md · ADR-20260721T192218Z-persistent-visual-iteration |
| CAP-0 | **Renderer build-graph contract:** keep normal game/cold-capture/preview renderers on one dynamic Bevy fingerprint and the persistent hotpatch host on one intentional static fingerprint; keep the capture control plane independent of the renderer graph | done | S | compile-observed: lightweight CLI 0.48 s; dynamic preview fingerprint shared; host graph isolated | build_speed.md · capture.md · ADR-20260721T194629Z-first-class-headless-capture-runtime |
| CAP-1 | **Shared game runtime + thin interactive app:** current game composition lives behind `thalos_runtime::AppBuilder`; `apps/game` is a thin `thalos_game` launcher | verify | L | game + capture-host checks clean; **user:** normal interactive launch | capture.md §7 · ADR-20260721T194629Z-first-class-headless-capture-runtime |
| CAP-2 | **Typed capture protocol/runtime + headless host:** typed request/result crate and dedicated host have landed; continue extracting preset/camera/F2/F8/server/readback systems from the shared runtime without a circular dependency | wip | L | CAP-1 | capture.md §3–§4, §7 |
| CAP-3 | **Rust capture CLI + artifact contract:** Rust controller and comparison binary now own persistent/cold orchestration; finish one-binary subcommands, fatal pipeline validation, and `artifacts/` migration | wip | M–L | CAP-2 | capture.md §5–§7 |
| CAP-4 | **Deterministic headless video:** fixed capture clock, body-fixed/inertial camera tracks, readback ring, background lossless frame writer, validated/resumable sequences and external FFmpeg MP4/WebM muxing through the same `CaptureSpec` and renderer | later | L | CAP-3 | capture.md §5, §7 |
| ARCH-TEX | **Make texture generation truly offline:** `thalos_texgen` is under `crates/offline`; `tools/texgen` emits versioned vegetation PNGs + manifest; rendering embeds those baked assets and has no texgen dependency | verify | M | compile clean; headless object/terrain capture still required | architecture.md · ADR-20260721T194628Z-role-based-agent-first-workspace |
| ARCH-SEAMS | **Repair package dependency direction:** construction→rendering is flipped; compatibility material re-exports are deleted; the shared height/query/patch contract lives in terrain; local physics no longer depends on rendering | done | L | compile-verified 2026-07-21 | architecture.md |
| BL-32 | **Finish the role-based workspace relocation:** role folders, the player-facing game under `apps/`, developer/offline executables (including the headless host) under `tools/`, vendor placement, nested erosion, offline sources, generated-evidence layout, and stale-path cleanup have landed. There is no `labs/` layer; visual verification scenes belong to runtime capture. | done | M–L | compile-verified 2026-07-21 | architecture.md · ADR-20260721T194628Z-role-based-agent-first-workspace · ADR-20260721T211446Z-player-facing-apps-developer-tools |
| CAP-SCENES | **In-context UI/object verification scenes:** replace the temporary preview examples with deterministic runtime presets for real UI screens, a UI gallery, and procedural-object/scatter galleries through the canonical capture host | later | M | CAP-2 | capture.md · visual_testing.md |
| BUILD-PAR | **Stable parallel-agent build substrate:** portable dev profiles live in `Cargo.toml`; generated local Cargo config owns fast linker, always-on dependency sccache, and a bounded per-process job budget; parallel agents use isolated worktree targets plus one shared cache | done | S | config/scripts/docs verified 2026-07-21 | build_speed.md · ADR-20260721T212438Z-portable-build-policy-local-acceleration |
| BUILD-ACT | **Make build acceleration directory-independent and non-silent:** BUILD-PAR's activation lived in a gitignored per-checkout `.cargo/config.toml`, so worktrees outside the tree built with no sccache and no rust-lld, and Windows had no way to provision a worktree at all; the job budget also halved the machine for the solo case. Activation moved to a global `RUSTC_WRAPPER`; job budget defaults to 1 Cargo process; `-SyncOnly`/`-AllWorktrees` added; roots sorted longest-first; platform-correct `SCCACHE_BASEDIRS` separator; `check-build-env.sh` now verifies every worktree's normalization with canonical path comparison | verify | S | scripts parse/syntax clean and checker re-run 2026-07-22 (it now names the 2 stale worktrees). **User:** run `scripts\setup-build-env.ps1 -Force -AllWorktrees` once builds are idle, then confirm `check-build-env.sh` is READY | build_speed.md §5.0/§5.3.1 · CLAUDE.md · INC-0019 |

## Decisions pending

Forks that gate queued work. Resolving one = an ADR + flipping this row
(steer Mode 2).

| Fork | Gates | Where the options live |
|------|-------|------------------------|
| N-craft `Simulation` API shape (multi-craft records vs keyed map) | CL-E2 | clean §2.2 — decision needed, implementation deliberately deferred |
| Q2 — tonemapper base (AgX vs PBR Neutral) | C1 | gfx §7 |
| Q5 — VegLayer driver unification shape | VEG-R | gfx §7 |
| Q6 — Slice-6 distant-body path | W17 | gfx §7 |
| Q8 — sky-view LUT: sun-only vs per-dominant-light | F7/F9 polish | gfx §3 F3 caveat |
| Q9 — does terrain move onto Bevy's default render path (`ExtendedMaterial`) or stay a custom UDLOD pipeline? | BL-35, and how far F7–F9 push crafts onto the spine | ADR-20260722T105147Z-tile-native-surface-seam — **deliberately deferred**; trigger = package schema frozen + one body producing real tiles + `verify` queue clear. Answered by the BL-35 Mira slice, not by argument |
| Q10 — terrain package storage: pixel-space heights vs latent + on-device decode (~16× smaller, perceptually-optimised loss, but decode latency enters the streaming budget and colliders need decoded metres) | MIRA-2/3 package schema freeze | terrain_diffusion_evaluation.md §3.4 *(terrain branch)* |
