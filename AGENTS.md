# Agent operating manual

Cursor loads both `AGENTS.md` and `CLAUDE.md` into every session. This file
is a pointer, not a copy — duplicating the manual doubled the always-on tax
(ADR-20260819T065557Z).

**Read [`CLAUDE.md`](CLAUDE.md)** before working: current direction,
verification contract, and the invariants that are expensive to rediscover.

Need the docs map, crate anatomy, or a subsystem spec? Start at
[`docs/README.md`](docs/README.md). What's next? `just queue`.
