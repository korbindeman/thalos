/target
/stage-bakes
# Rendered output of the procedural-object previewer (`just preview`).
/tools/preview/out/
# Headless whole-scene captures (`just screenshot` / THALOS_SCREENSHOT).
/tools/screenshots/
# Disposable A/B matrices and ad-hoc agent capture iterations.
/tools/agent_scratch/*
!/tools/agent_scratch/README.md
# Machine-readable runtime diagnostics (JSONL and related probe output).
/tools/diagnostics/
/tools/ui_preview/
# Baked procedural textures (`cargo run -p thalos_texgen --example bake`).
/tools/texgen/out/
# Per-user runtime settings written by the game (window mode, UI scale, …).
/user/
Cargo.lock
.DS_Store
.cargo/config
.cargo/config.toml
.env.just
.thalos_dumps/
# Session-local Claude state stays ignored, but project skills (steer,
# wgsl-bevy) are shared infrastructure and are tracked.
.claude/*
!.claude/skills/
.codex/
crates/ui/tools/ui_preview/kitchen_sink.png
rustc-ice-2026-07-20T17_25_42-22912.txt
