//! Shared HUD visual style — moved to `thalos_ui::hud_theme` (Phase 5b) so
//! feature crates style panels without a runtime dependency. Re-exported for
//! path stability.

pub use thalos_ui::hud_theme::*;
