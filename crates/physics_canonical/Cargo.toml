[package]
name = "thalos_physics_canonical"
version.workspace = true
edition.workspace = true

[dependencies]
glam = { version = "0.30.10", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
tracing = "0.1"
thalos_world = { path = "../world" }

[dev-dependencies]
approx = "0.5"
