[package]
name = "thalos_terrain_learned"
publish = false
version.workspace = true
edition.workspace = true

[dependencies]
burn = { workspace = true, features = ["std"] }
serde = { version = "1", features = ["derive"] }
