[package]
name = "thalos_capture_protocol"
publish = false
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thalos_render_model = { path = "../../rendering/model" }

[dev-dependencies]
serde_json = "1.0"
