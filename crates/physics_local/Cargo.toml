[package]
name = "thalos_physics_local"
version.workspace = true
edition.workspace = true

[dependencies]
avian3d = { version = "0.6.1", default-features = false, features = ["3d", "f64", "parry-f64", "parallel"] }
bevy = { version = "0.18", features = ["wayland", "jpeg"] }
thalos_physics_canonical = { path = "../physics_canonical" }
thalos_terrain_render = { path = "../terrain_render" }
thalos_terrain = { path = "../terrain" }

[dev-dependencies]
approx = "0.5"
