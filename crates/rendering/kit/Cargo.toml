[package]
name = "thalos_render_kit"
publish = false
version.workspace = true
edition.workspace = true

[dependencies]
bevy = { version = "0.19", features = ["wayland", "jpeg"] }
thalos_render_model = { path = "../model" }
