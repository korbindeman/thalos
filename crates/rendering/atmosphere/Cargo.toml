[package]
name = "thalos_atmosphere"
publish = false
version.workspace = true
edition.workspace = true

[dependencies]
bevy = { version = "0.19", features = ["wayland", "jpeg"] }
thalos_world = { path = "../../domain/world" }
