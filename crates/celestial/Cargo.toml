[package]
name = "thalos_celestial"
version.workspace = true
edition.workspace = true

[dependencies]
glam = { version = "0.30.10", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
rand = { version = "0.10", default-features = false, features = ["std", "std_rng"] }
rand_chacha = "0.10"
rand_distr = "0.6"
rayon = "1.12"

[dev-dependencies]
approx = "0.5"
